#!/bin/bash

source "$HOME/.config/bash/tmux_running.sh"

launch() {
  if tmux_window_exists MyVim; then
    tmux select-window -t MyVim
  else
  tmux new-window -n MyVim \
    && tmux send-keys -t :MyVim 'myvim' Enter
  fi
}

tmux_ready && launch
